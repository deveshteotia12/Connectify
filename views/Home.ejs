<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@1,700&display=swap" rel="stylesheet">
    <link href="./css/index.css" rel="stylesheet">
</head>
<body>
    <div class="homeContainer">
    <div class="homeBox1">
        <div class="circle"></div>
        <h1><span class="whiteColor" style="color: white;">C</span>onnectify</h1>
        <p>We help you to <span class="blue" id="content"></span></p>
    </div>
    <div class="homeBox2">
        <a href="/login">Login</a>
        <a href="/signUp">SignUp</a> 
    </div> 
   </div> 
   <script>
            var i=0;
            var txt="Connect"
            var txt1="Create"
            var txt2="Conquer"
            var speed=300;
            function typeWriter()
            {
                if(i<txt.length)
                {
                    document.getElementById("content").innerHTML+=txt.charAt(i);
                    i++;
                    setTimeout(typeWriter,speed);
                }
                else if(i==txt.length)
                {
                    i=0;
                    setTimeout(()=>document.getElementById("content").innerHTML="",900)
                    setTimeout(typeWriter2,900)   
                }
            }
            function typeWriter2()
            {
                if(i<txt1.length)
                {
                    document.getElementById("content").innerHTML+=txt1.charAt(i);
                    i++;
                    setTimeout(typeWriter2,speed);
                }
                else if(i==txt1.length)
                {
                    i=0;
                    setTimeout(()=>document.getElementById("content").innerHTML="",900)
                    setTimeout(typeWriter3,900)   
                }
            }
            function typeWriter3()
            {
                if(i<txt2.length)
                {
                    document.getElementById("content").innerHTML+=txt2.charAt(i);
                    i++;
                    setTimeout(typeWriter3,speed);
                }
                else if(i==txt2.length)
                {
                    i=0;
                    setTimeout(()=>document.getElementById("content").innerHTML="",900)
                    setTimeout(typeWriter,902)   
                }
            }
            typeWriter();
    </script>
</body>
</html>